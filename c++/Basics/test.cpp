#include<iostream>
#include <cctype>
using namespace std;
int main(){
    // if(!(cout<<"hello"))
    // cout<<"world";
    // else
    // cout<<"nothing";
    int n;
    cout<<"Enter a number : ";
    cin>>n;
    cout<<abs(n);
    // char ch;
    // cout<<"Enter a character : ";
    // cin>>ch;
    // cout<<isalpha(ch);
    return 0;
}