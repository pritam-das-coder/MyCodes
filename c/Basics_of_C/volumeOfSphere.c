#include<stdio.h>
int main(){
    // float r=1;
    // float pi=3.14;
    // float vol=(4.0/3)*pi*r*r*r;
    // printf("The volume of sphere is %f unit cube",vol);
    float r;
    printf("Enter the radius of circle : ");
    scanf("%f",&r);
    float area=3.1415*r*r;
    printf("The area of circle is %f unit square",area);
    return 0;
}