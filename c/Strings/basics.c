#include <stdio.h>
#include <string.h>
int main(){
    // char str[]="Pritam is GOAT";
    char* str="Hello";
    char* st="World";
    int l=strcmp(str,st);
    printf("%d",l);
    // printf("%s\n",*st);
    // printf("%s\n",*str);
    // char str[10];
    // scanf("%s",&str);
    // gets(str);
    // printf("%c",str[0]);
    // puts(str);
    // char str[13]="PhysicsWallah";
    return 0;
}
