#include <stdio.h>
int main(){
    // int a=5,b,c;
    // b=a=15;
    // c=a<15;
    // printf("\na=%d b=%d c=%d",a,b,c);
    // int x=15;
    // printf("\n%d %d %d",x!=15,x=20,x<30);
    int a = 300, b, c ; 
    if ( a >= 400 ) 
    b = 300 ; 
    c = 200 ; 
    printf ( "\n%d %d", b, c ) ; 
    return 0;
}